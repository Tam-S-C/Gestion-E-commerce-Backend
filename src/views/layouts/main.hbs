<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="/img/favi.ico" type="image/x-icon">
    <title>E-commerce</title>
    <link rel="stylesheet" href="/styles/home.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
</head>
<script>
  document.addEventListener("DOMContentLoaded", () => {
    let cartId = sessionStorage.getItem("cartId");

    if (!cartId) {
      fetch("/api/carts", { method: "POST" })
        .then((res) => res.json())
        .then((data) => {
          cartId = data.cart._id;
          sessionStorage.setItem("cartId", cartId);
          document.getElementById("cart-link").href = `/carts/${cartId}`;
        })
        .catch((error) => console.error("Error al crear el carrito:", error));
    } else {
      document.getElementById("cart-link").href = `/carts/${cartId}`;
    }
  });
</script>

<body>
<header>
  <nav class="navbarBTN">
    <figure> 
      <img src="/img/favi.ico" alt="Imagen de Carrito de Compras" width="36" height="36">
      <span>E-Papelería-Geek</span>
    </figure>
    <a href="/" class="btn btn-secondary">Home</a>
    <a class="btn btn-secondary" href="/products">Productos con Mongo</a>
    <a class="btn btn-secondary" id="cart-link">Carrito</a>

  </nav>
</header>

<div class="container">
    {{{ body }}}    
</div>

<footer>
    <p>Copyright 2025 - Tam S.C.</p>
</footer>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"
    ></script>
</body>
</html>